---
description: Research a library, API, error, or concept and return a focused actionable summary
---
Research the following and return only what's needed to act on it:

1. Search with `web_search` using a specific query.
2. Fetch the best 2–3 results with `web_fetch`.
3. Extract the precise answer — API signature, config option, error cause, or pattern.
4. Note the source URL.
5. If it's worth remembering, call `remember("codebase", ...)`.

Topic: $@
